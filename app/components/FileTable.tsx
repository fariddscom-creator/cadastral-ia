'use client';

import React from 'react';

interface FileDistrict {
  fileName: string;
  status: string;
  commune?: string;
}

export function FileTable({ files }: { files: FileDistrict[] }) {
  return null;
}
